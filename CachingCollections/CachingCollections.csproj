<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <Title>Caching-Collections</Title>
    <Description>A .NET Standard 2.1 library intended to help developers be performance-minded when working with in-memory IEnumerables and ICollections.

The library is intended to minimize the number of times a collection is enumerated, uses caching for quick-retrieval of items within a collection, and automatically arranges filter ordering to reduce the amount of work of a query.</Description>
    <Copyright>2024, Eric S. Li</Copyright>
    <PackageReleaseNotes>Initial alpha release.  Functional, but not thoroughly tested, and not performance tested.</PackageReleaseNotes>
    <AssemblyVersion>0.0.0.0-alpha</AssemblyVersion>
    <FileVersion>0.0.0.1</FileVersion>
    <NeutralLanguage>en</NeutralLanguage>
    <PackageTags>IEnumerable, Enumerable, ICollection, IList, IReadOnlyCollection, Where, Query, Filter, LINQ, Caching</PackageTags>
  </PropertyGroup>

</Project>
